import React from 'react';
import './Header';

const Header = () => {
    return (
        <div class='fixheader'>
            <img alt='logo' src=''></img>
            <button>Home</button>
            <button>About</button>
            <button>Contact</button>
        </div>
    );
}

export default Header;